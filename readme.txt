LEXER // TODO show `'` token on error | check leaks
		$(RM) $(OBJ) // relink makefile
		// remove printing of tokens
		// make the token words instead of just charachters
lexer -> parser -> expender -> executor

